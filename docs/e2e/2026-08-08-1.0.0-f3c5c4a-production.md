# Authorized Production Live E2E — 1.0.0 / f3c5c4a

## Verdict

**Pass.** This run qualifies as the live provider evidence for `stable` under explicit project-owner authorization to use the production environment. The production account was already disabled before the release-candidate write check and intentionally remained disabled; the candidate performed an idempotent, confirmed write and re-read verification without changing business state.

## Provenance

| Field | Recorded value |
|-------|----------------|
| Date | 2026-08-08 |
| CLI version | `1.0.0` |
| Candidate commit | `f3c5c4aae1f07673951284ce850d9afe266147fd` |
| Candidate binary SHA-256 | `461C82AC31977FFF73AA5F922115CFEAAE65AC6A490666CA5996CA93AA817A85` |
| Build | `go build -trimpath`; `vcs.modified=false` |
| Platform | Windows amd64, Go 1.26.5 |
| CLIProxyAPI version | `v7.2.114` |
| CLIProxyAPI tag commit | `41fc5e134631789e98137245f576680c7fb9b322` |
| Management base | `https://<production-host>/v0/management` |
| Credential source | OS keyring |

The deployed CLIProxyAPI version was read from the `X-Cpa-Version` response header. The tag commit was resolved from the official `router-for-me/CLIProxyAPI` GitHub repository.

## Sanitized Results

| Check | Result |
|-------|--------|
| `reference` | Pass; 11 leaf commands. The account-status command declared `permission_tier=dangerous`, `write_gate=dangerous_dry_run_confirm`, required `--dangerous`, and documented dangerous dry-run/confirm examples. |
| `context` | Pass; configured keyring credential, HTTPS Management target, Windows amd64, Go 1.26.5. The key was not emitted. |
| `doctor` before promotion | Configuration, credentials, and Management API passed. Release readiness warned as expected because the evidence had not yet been recorded. |
| `auth-file list` | Pass; 38 records: 37 Codex and 1 Claude. Full pagination completed. |
| `quota inspect --provider codex` | Pass; 37/37 probes succeeded with upstream HTTP 200. Assessments: 36 healthy and 1 confirmed exhausted. |
| `guard run-once` | Pass; observation-only. 37 decisions were skipped safely: 35 not exhausted and 2 already disabled. No write was attempted. |
| Missing dangerous gate | Pass; the candidate rejected status preview with `E_USAGE` before any confirmation or write. |
| Dangerous dry-run/confirm | Pass; the exact redacted account alias was previewed with `current_disabled=true` and `target_disabled=true`, confirmed, and returned `verified=true`. |
| Post-write re-read | Pass; exactly one target matched and remained `disabled=true`, `status=disabled`. |
| `_untrusted` contract | Pass; auth, quota, and guard results declared their external-data paths. |

## Write Scope

- Authorization: explicit project-owner approval in the release task.
- Target: one redacted production account alias; no email, filename, or `auth_index` is retained here.
- Requested and final state: disabled.
- Business-state change during the candidate run: none.
- Restoration: not applicable; enabling the account would have contradicted the requested final production state.

An earlier production state transition used an untracked binary without VCS provenance and is not used as release-candidate evidence. This recorded run uses only the clean, SHA-bound candidate above. Live enable transition coverage remains intentionally out of production scope and is covered by the existing mock/contract status round-trip.

## Redaction

This artifact contains no Management key, provider token, cookie, authorization header, raw auth file, confirmation token, production hostname, account identifier, email address, filename, or `auth_index`. Only aggregate counts and redacted aliases are retained. Command output was summarized instead of copied verbatim so untrusted provider strings could not enter the repository. Gitleaks `v8.30.1` found no leaks in either the completed working tree or Git history.
