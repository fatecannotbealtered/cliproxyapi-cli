# Authorized Production Targeted Quota Smoke — 1.0.1 / 71bc3de

## Verdict

**Pass.** This explicitly authorized, read-only production smoke validates the `1.0.1` quota-semantics regression fix. It supplements the qualifying `1.0.0` full E2E; it did not exercise or authorize account writes.

## Provenance

| Field | Recorded value |
|-------|----------------|
| Date | 2026-08-09 |
| CLI version | `1.0.1` |
| Candidate commit | `71bc3de68298d0c64283b580604256cf38d2668c` |
| Candidate binary SHA-256 | `99D08A8BD964AEAE21BAE85A45DDB43B3A5F4C485BE1A9DEA7E1C5534DD7432F` |
| Build | `go build -trimpath` from the clean candidate commit |
| Platform | Windows amd64, Go 1.26.5 |
| CLIProxyAPI deployment | Same authorized production target as the qualifying `1.0.0` E2E; version was not re-collected |
| Management base | `https://<production-host>/v0/management` |
| Credential source | OS keyring |

## Sanitized Results

| Check | Result |
|-------|--------|
| `reference` | Pass; 11 leaf commands. |
| `context` | Pass; version `1.0.1`, configured keyring credential, and no emitted key. |
| `doctor` | Pass; configuration, credentials, Management API, and release-readiness checks all passed. |
| `quota inspect --provider codex` | Pass; 37/37 probes succeeded and all 37 assessments were healthy. |
| Weekly percentage semantics | Pass; 35 windows reported `used=0`, `remaining=100`; one reported `1/99`; one reported `2/98`. There were zero item- or window-level derivation mismatches. |
| Reset diversity | 36 distinct weekly reset timestamps, consistent with per-account routing rather than one duplicated response. |
| Account writes | None attempted. |

## Scope and Redaction

This targeted smoke verifies only the changed quota probe and output semantics. The `1.0.0` full E2E remains the evidence for guard behavior and dangerous dry-run/confirm writes.

This artifact contains no Management key, provider token, cookie, authorization header, raw auth file, confirmation token, production hostname, account identifier, email address, filename, or `auth_index`. Only aggregate counts and the candidate provenance are retained.
