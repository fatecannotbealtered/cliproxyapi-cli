version: 2

project_name: cliproxyapi-cli

before:
  hooks:
    - go mod tidy

builds:
  - main: ./cmd/cliproxyapi-cli
    binary: cliproxyapi-cli
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X github.com/fatecannotbealtered/cliproxyapi-cli/cmd.version={{ .Version }}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - formats:
      - tar.gz
    format_overrides:
      - goos: windows
        formats:
          - zip
    name_template: "{{ .ProjectName }}-{{ .Version }}-{{ .Os }}-{{ .Arch }}"
    wrap_in_directory: false
    # Ship CLI binary only — skills install via npx.
    files:
      - none*

checksum:
  name_template: checksums.txt

signs:
  - cmd: cosign
    artifacts: checksum
    signature: "${artifact}.sigstore.json"
    args:
      - "sign-blob"
      - "--new-bundle-format"
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"

release:
  # Idempotent re-tagging: overwrite artifacts if the release already exists.
  replace_existing_artifacts: true
